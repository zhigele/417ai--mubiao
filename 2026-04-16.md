# 2026-04-16

## YOLOv8 目标检测系统

### 项目文件
- `app.py` — Flask 全栈应用（前端+后端+管理后台）
- `requirements.txt` — Python 依赖

### 登录 + 管理后台
- 数据库：MySQL test2库，root/123456
- 自动建表+升级：`init_db()` 自动创建/升级 users、detection_logs 表
- 默认管理员：`admin` / `admin123`（启动时自动创建）
- Session 鉴权：admin 登录 → /admin，普通用户 → /
- 管理后台：统计面板、用户管理、检测记录、系统设置

### AI 助手聊天功能（新增）
- 右下角悬浮 💬 按钮，检测页和管理后台均可用
- API：`POST /api/chat`，内置 YOLOv8 知识库（Q&A 匹配）
- 覆盖：上传模型、置信度/IoU 参数、检测排查、训练导出、GPU 加速等
- 快捷问题按钮（4个），消息本地存储，清空历史功能

### 依赖
- `pymysql>=1.1.0`、`cryptography`
